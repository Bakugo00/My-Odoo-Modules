<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="hr_contract_view_form" model="ir.ui.view">
        <field name="name">hr.contract.view.form</field>
        <field name="model">hr.contract</field>
        <field name="inherit_id" ref="hr_contract.hr_contract_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='department_id']" position="after">
                <field name="partner_id" />
            </xpath>
            <xpath expr="//field[@name='date_end']" position="after">
                <field name="end_trial_prd_date" />
            </xpath>
        </field>
    </record>
    <record id="hr_contract_view_kanban" model="ir.ui.view">
        <field name="name">hr.contract.view.kanban</field>
        <field name="model">hr.contract</field>
        <field name="inherit_id" ref="hr_contract.hr_contract_view_kanban" />
        <field name="arch" type="xml">
            <xpath expr="//div[@class='o_hr_contract_state']" position="before">
                <div class="o_hr_contract_state">
                    <strong class="o_kanban_record_title">
                        <field name="employee_id"/>
                    </strong>
                </div>
            </xpath>
        </field>
    </record>
</odoo>
