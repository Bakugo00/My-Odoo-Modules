<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="sale_account_move_form_inherit" model="ir.ui.view">
            <field name="name">sale.account.move.form.inherit</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="sale.account_invoice_form"/>
            <field name="arch" type="xml">

                <xpath expr="//group[@name='sale_info_group']/field[@name='team_id']" position="attributes">
                    <attribute name="options">{'no_create': True, 'no_create_edit': True,'no_quick_create': True ,
                        'no_open': True}
                    </attribute>
                </xpath>

                <xpath expr="//notebook/page[1]" position="after">
                    <page name="attendees_ids" string="Participants" attrs="{'invisible': [('move_type', '=', 'entry')]}" groups='sales_team.group_sale_salesman'>
                        <field name="attendees_ids" attrs="{'readonly': [('state', 'in', ('posted', 'cancel'))]}">
                            <tree editable="bottom">
                                <field name="user_id"
                                       options="{'no_quick_create':True,'no_create_edit':True,'no_create_edit': True, 'no_open': True}"/>
                                <field name="participation" sum="Total"/>
                                <field name="amount_participant" sum="Montant" widget="monetary"
                                       options="{'currency_field': 'currency_id'}"/>
                                <field name="currency_id" invisible="1"/>
                            </tree>
                            <form>
                                <sheet>
                                    <group>
                                        <field name="user_id" options="{'no_create': True, 'no_create_edit': True,'no_quick_create': True ,
                        'no_open': True}"/>
                                        <field name="participation"/>
                                        <field name="amount_participant"/>
                                        <field name="currency_id" invisible="1"/>
                                    </group>
                                </sheet>
                            </form>
                        </field>
                    </page>
                </xpath>

            </field>
        </record>


    </data>
</odoo>